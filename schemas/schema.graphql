type Query {
    hello: String
    data(a: Int!, b: String): [String]
    user(username: String)
    issue(id: String): Issue
}

type Issue {
    title: String
    body: String
    bodyRendered: String
    comments: [Comment]
}

type Comment {
    author: User
    text: String
    textRendered: String
}

type User {
    name: string
    components: [Component]
    projects: [Project]
}

type Component {
    name: string
    issues: [Issue]
    projects: [Project]
}

type Project {
    name: string
    components: [Component]
}